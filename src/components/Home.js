import React from "react"
import axios from "axios"

export default class Home extends React.Component{
    render(){
        return(
            <React.Fragment>
                <div>
                    
                </div>
            </React.Fragment>
        )
    }
}